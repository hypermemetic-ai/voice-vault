<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ai.hypermemetic.voicevault"
    android:versionCode="6"
    android:versionName="1.2.2">

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.VoiceVault"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true">

        <!-- Primary 0ms Transparent Target for Pixel Quick Tap & Hardware Triggers -->
        <activity
            android:name=".ToggleDictationActivity"
            android:exported="true"
            android:label="@string/quick_tap_name"
            android:theme="@style/Theme.VoiceVault.Transparent"
            android:excludeFromRecents="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full UI Dashboard with History Slide-Out Drawer -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/transcripts_title"
            android:theme="@style/Theme.VoiceVault"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Voiceprint Enrollment Flow (server-side speaker gate) -->
        <activity
            android:name=".VoiceEnrollActivity"
            android:exported="false"
            android:label="@string/enroll_title"
            android:theme="@style/Theme.VoiceVault"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Core Background Recording Service -->
        <service
            android:name=".VoiceVaultService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- Quick Settings Tile -->
        <service
            android:name=".VoiceVaultTileService"
            android:label="@string/tile_name"
            android:icon="@drawable/ic_mic"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Hardware Side-Button (Volume Down double-tap) Accessibility Service -->
        <service
            android:name=".VoiceVaultKeyService"
            android:label="Voice Vault Side Button"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Optional Floating Screen Edge Bubble -->
        <service
            android:name=".FloatingBubbleService"
            android:exported="false" />
    </application>
</manifest>
